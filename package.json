{
  "name": "watcherforte",
  "version": "1.0.0",
  "description": "WatcherForte - Automated NFT monitoring platform on Flow blockchain",
  "private": true,
  "workspaces": [
    "frontend",
    "telegram-notifier"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend && node -e \"require('fs').cpSync('frontend/dist', 'dist', {recursive: true, force: true})\"",
    "build:frontend": "npm run build --workspace=frontend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "start:api": "npm start --workspace=telegram-notifier",
    "dev:api": "npm run dev --workspace=telegram-notifier"
  }
}

